const config = {
    apiKey: "YOUR_KEY",
    authDomain: "PROJECT_DOMAIN",
    databaseURL: "DATABASE_URL",
    projectId: "projectId",
    storageBucket: "URL",
    messagingSenderId: "ID"
};

export default config;